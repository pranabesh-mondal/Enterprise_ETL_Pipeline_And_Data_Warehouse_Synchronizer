# =====================================================================
# Enterprise ETL Pipeline & Data Warehouse Synchronizer
# Python 3.11+ dependencies
# =====================================================================

# ------------------------- Data Processing -------------------------
pandas>=2.2.0
polars>=1.0.0
numpy>=1.26.0

# --------------------- Validation & Config -------------------------
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# --------------------- Extraction / Networking ---------------------
requests>=2.31.0
tenacity>=8.2.3

# ----------------------- Storage / Warehouse -----------------------
boto3>=1.34.0                      # AWS S3 raw data lake
SQLAlchemy>=2.0.25
psycopg2-binary>=2.9.9             # PostgreSQL driver
snowflake-sqlalchemy>=1.30.0       # Snowflake support (optional target)
snowflake-connector-python>=3.10.0

# ------------------------- Orchestration ---------------------------
# NOTE: For production Airflow installs, use the official constraints file:
#   pip install "apache-airflow>=2.9.0" --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.11.txt"
apache-airflow>=2.9.0

# --------------------------- Alerting ------------------------------
slack-sdk>=3.27.0

# ----------------------------- Testing -----------------------------
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
responses>=0.25.0                  # Mock HTTP requests in tests
freezegun>=1.4.0                   # Freeze time for date-based tests
moto[s3]>=5.0.0                    # Mock AWS S3 in tests

# ------------------------ Code Quality / CI ------------------------
ruff>=0.3.0                        # Linting + formatting
black>=24.2.0
mypy>=1.8.0
pre-commit>=3.6.0
